// Controller placeholder - wire up routes here in future
import { Request, Response } from "express";

export const placeholder = (req: Request, res: Response) => {
  res.status(200).json({ message: "placeholder" });
};
